[project]
name = "freshstack-acceptance-project"
version = "0.1.0"
description = "Acceptance test application for FreshStack verification"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.0",
    "pydantic==2.9.2",
    "sqlalchemy==2.0.35",
    "alembic==1.13.3",
]

[tool.uv]
