[project]
name = "sample-service"
version = "0.1.0"
description = "A sample FastAPI microservice"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "pydantic>=2.8.0",
    "sqlalchemy>=2.0.0",
    "alembic>=1.13.0",
]

[tool.uv]
dev-dependencies = ["pytest"]
